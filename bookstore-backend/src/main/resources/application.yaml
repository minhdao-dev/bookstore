spring:
  application:
    name: bookstore-backend
  jpa:
    hibernate:
      ddl-auto: validate
  flyway:
    enabled: true
    locations: classpath:db/migration
app:
  jwt:
    secret: ${JWT_SECRET:this-is-a-dev-only-placeholder-secret-change-me-32chars+}
    expiration-ms: 1800000