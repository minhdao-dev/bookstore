/**
 * Catalog & Search module — book metadata (author, genre) and product
 * variants (ebook, audiobook, physical — softcover/hardcover). Each
 * variant owns its own SKU, price and product_type (digital/physical).
 * Physical variants additionally carry weight/dimensions.
 */
package com.bookstore.catalog;
